---
description: Brief description of what this command does
---

# Example Command

This is a template command. Replace this content with instructions for Claude.

## Usage

The user will invoke this with: `/plugin-name:example`

## Arguments

Use `$ARGUMENTS` to capture user input:
- Example: `/plugin-name:example some input here`

## Instructions

Tell Claude what to do when this command is invoked:

1. First, analyze the user's request
2. Then, perform the necessary actions
3. Finally, provide a helpful response

$ARGUMENTS
